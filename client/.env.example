# Client environment variables
# For local development:
VITE_API_URL=http://localhost:3000

# For Docker: VITE_API_URL is a build-time variable, passed via
# docker-compose.yml build args. No .env file needed in Docker.
