# Server environment variables
# For local development:
PORT=3000
DATABASE_PATH=./data/todo.db
CORS_ORIGIN=http://localhost:5173

# For Docker (set via docker-compose.yml — no .env needed):
#   PORT=3000
#   DATABASE_PATH=./data/todo.db
#   CORS_ORIGIN=http://localhost:5173
#   NODE_ENV=production
